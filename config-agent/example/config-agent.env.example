# Config Agent credentials — keep this file root:root 0600.
# Generate independent random values; do not reuse either value.
CONFIG_AGENT_SECRET=CHANGE_ME_TO_A_DIFFERENT_RANDOM_SECRET_AT_LEAST_32_CHARS
CONFIG_AGENT_API_TOKEN=CHANGE_ME_TO_A_LONG_RANDOM_API_TOKEN_AT_LEAST_32_CHARS

# Optional non-secret runtime settings:
# LOG_LEVEL=info
# SYSTEMCTL_FLAGS=
